# HINWEIS: Dies ist die finale, syntaktisch korrekte Version der fly.toml,
# die die Build-Prozesse für Node.js und Python korrekt definiert.

app = 'wasserqualitaet-vg-bitter-frost-7826'
primary_region = 'fra'

[build]
  # --- KORREKTE SYNTAX ---
  # Wir geben dem Builder eine einfache Liste der benötigten Sprach-Pakete.
  buildpacks = ["heroku/nodejs", "heroku/python"]

[env]
  PORT = "8080"

[mounts]
  # Ihre Volume-Konfiguration bleibt exakt erhalten
  source = "mailer_data"
  destination = "/data"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[processes]
  app = "npm start"

[[cron]]
  schedule = "0 5 * * *"
  command = "node send-digest.js"